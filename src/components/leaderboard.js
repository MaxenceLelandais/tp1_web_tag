

export default function Leaderboard() {
    const wrapper = $('<div></div>').addClass('wrapper');
    const leaderboardSection = $('<div></div>').addClass('leaderboardSection');
    const leaderboardtabs = $('<div></div>').addClass('leaderboardtabs');
    const leaderboard = $('<div></div>').addClass('leaderboard');
    const ulli = $('<ul></ul>');
    const lien = $('<li></li>');
    const lien1 = $('<li></li>');
    const lien2 = $('<li></li>');
}
